import React from "react";

const absoluteStyles = {
  zIndex: "100000000",
  margin: "0",
  position: "fixed",
  bottom: "0",
  left: 0,
  margin: "1rem"
};

const defaultStyles = {
  margin: "1rem"
};

export default ProducHuntWidget;
